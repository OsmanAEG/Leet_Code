#include <vector>

class Solution {
public:
  template<typename Dir_T>
  bool valid_move(std::vector<std::vector<int>>& grid, Dir_T& dir, int& i, int& j) {

  }

  bool hasValidPath(std::vector<std::vector<int>>& grid) {
    bool is_deadend = false;
    bool is_valid = false;

    while(!is_deadend || !is_valid) {

    }

    return is_valid;
  }
};